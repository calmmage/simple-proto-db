from setuptools import setup

setup(
    name='simple-proto-db',
    version='0.1.0',
    description='',
    author='Man Foo',
    author_email='foomail@foo.com',
    packages=['foo'],  # same as name
    install_requires=['bar', 'greek'],  # external packages as dependencies
)
